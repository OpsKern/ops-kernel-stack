[Unit]
Description=Nightly restic backup on %H (with jitter)

[Timer]
OnCalendar={{ restic_oncalendar | default('*-*-* 03:00:00') }}
RandomizedDelaySec={{ restic_jitter | default('30m') }}
Persistent=true
Unit=restic-backup.service

[Install]
WantedBy=timers.target
