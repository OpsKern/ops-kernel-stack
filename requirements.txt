# Python dependencies for running tests and linting locally.
# Install: pip install -r requirements.txt

ansible>=10.0,<11.0
ansible-lint>=24.0,<25.0
molecule>=24.0,<25.0
molecule-plugins[docker]>=23.0
